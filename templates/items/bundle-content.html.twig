{% extends 'base.html.twig' %}

{% block body %}
    {{ include('layout/header.html.twig')}}
    <div class="col">
        <div class="bundleContent-container">
            {% for item in items %}
                <div>
                    {{ include('items/item.html.twig', {'item': item}) }}
                </div>     
            {% endfor %}
        </div>
    </div>
{% endblock %}